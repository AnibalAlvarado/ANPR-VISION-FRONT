services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: angular_frontend
    ports:
      - "4200:80"
    networks:
      - backend-network

networks:
  backend-network:
    external: true
